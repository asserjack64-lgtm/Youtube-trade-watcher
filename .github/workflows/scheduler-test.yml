name: Scheduler Test

on:
  workflow_dispatch:
  schedule:
    - cron: "3-59/5 * * * *"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Scheduler heartbeat
        run: |
          echo "================================="
          echo "GITHUB SCHEDULER IS WORKING"
          echo "UTC time:"
          date -u
          echo "================================="
